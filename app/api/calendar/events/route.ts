import { NextResponse } from "next/server";import { z } from "zod";import { getDb } from "@/db";import { meetings,safetyRisks,users } from "@/db/schema";import { ApiError,apiError,requireMember } from "@/lib/api";import { audit } from "@/lib/audit";import { IntegrationProvider,usableAccessToken } from "@/lib/integrations";import { inArray,eq } from "drizzle-orm";import { trackProductEvent } from "@/lib/analytics";
const schema=z.object({provider:z.enum(["google","microsoft","in_person"]),projectId:z.uuid().optional(),title:z.string().min(3).max(160),description:z.string().max(3000).optional(),startsAt:z.iso.datetime(),endsAt:z.iso.datetime(),timezone:z.string().default("Europe/London"),location:z.string().max(300).optional(),attendees:z.array(z.object({email:z.email(),name:z.string().max(100).optional()})).max(100).default([]),online:z.boolean().default(true)}).refine(v=>new Date(v.endsAt)>new Date(v.startsAt),"End time must follow start time");
export async function POST(request:Request){try{const member=await requireMember(),input=schema.parse(await request.json()),db=getDb();const [creator]=await db.select({ageBand:users.ageBand}).from(users).where(eq(users.id,member.id)).limit(1);const attendeeRecords=input.attendees.length?await db.select({email:users.email,ageBand:users.ageBand}).from(users).where(inArray(users.email,input.attendees.map(a=>a.email.toLowerCase()))):[];const mixedAge=attendeeRecords.some(a=>a.ageBand==="teen_16_17")&&(creator?.ageBand!=="teen_16_17"||attendeeRecords.some(a=>a.ageBand!=="teen_16_17"));if(mixedAge&&(!input.projectId||input.attendees.length<2)){await db.insert(safetyRisks).values({subjectUserId:creator?.ageBand==="teen_16_17"?member.id:null,type:"adult_teen_meeting_blocked",severity:"high",details:{projectLinked:Boolean(input.projectId),attendeeCount:input.attendees.length}});throw new ApiError(403,"Adult and teen meetings must be project-linked groups of at least three people")}let providerEventId:string|undefined,joinUrl:string|undefined;if(input.provider!=="in_person"){const token=await usableAccessToken(member.id,input.provider as IntegrationProvider);if(input.provider==="google"){const response=await fetch("https://www.googleapis.com/calendar/v3/calendars/primary/events?conferenceDataVersion=1&sendUpdates=all",{method:"POST",headers:{authorization:`Bearer ${token}`,"content-type":"application/json"},body:JSON.stringify({summary:input.title,description:input.description,start:{dateTime:input.startsAt,timeZone:input.timezone},end:{dateTime:input.endsAt,timeZone:input.timezone},attendees:input.attendees.map(({email})=>({email})),location:input.location,...(input.online?{conferenceData:{createRequest:{requestId:crypto.randomUUID(),conferenceSolutionKey:{type:"hangoutsMeet"}}}}:{})})});if(!response.ok)throw new Error("Google Calendar did not accept the event");const event=await response.json() as {id:string;hangoutLink?:string};providerEventId=event.id;joinUrl=event.hangoutLink}else{const response=await fetch("https://graph.microsoft.com/v1.0/me/events",{method:"POST",headers:{authorization:`Bearer ${token}`,"content-type":"application/json"},body:JSON.stringify({subject:input.title,body:{contentType:"HTML",content:input.description??""},start:{dateTime:input.startsAt,timeZone:input.timezone},end:{dateTime:input.endsAt,timeZone:input.timezone},attendees:input.attendees.map(a=>({emailAddress:{address:a.email,name:a.name},type:"required"})),location:{displayName:input.location??""},isOnlineMeeting:input.online,onlineMeetingProvider:input.online?"teamsForBusiness":undefined})});if(!response.ok)throw new Error("Microsoft Calendar did not accept the event");const event=await response.json() as {id:string;onlineMeeting?:{joinUrl?:string}};providerEventId=event.id;joinUrl=event.onlineMeeting?.joinUrl}}
const [meeting]=await db.insert(meetings).values({projectId:input.projectId,createdBy:member.id,provider:input.provider,providerEventId,title:input.title,description:input.description,startsAt:new Date(input.startsAt),endsAt:new Date(input.endsAt),timezone:input.timezone,joinUrl,location:mixedAge?null:input.location,attendees:input.attendees}).returning();await audit(member.id,"meeting.created","meeting",meeting.id,{provider:input.provider,projectId:input.projectId});await trackProductEvent({actorId:member.id,ageBand:creator?.ageBand,event:"meeting_created",entityType:"meeting",entityId:meeting.id,properties:{provider:input.provider}});return NextResponse.json(meeting,{status:201})}catch(error){return apiError(error)}}
