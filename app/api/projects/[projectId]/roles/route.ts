import { NextResponse } from "next/server";import { and,eq } from "drizzle-orm";import { z } from "zod";import { getDb } from "@/db";import { projects,projectRoles } from "@/db/schema";import { ApiError,apiError,requireMember } from "@/lib/api";import { audit } from "@/lib/audit";
const schema=z.object({title:z.string().min(2).max(80),department:z.string().min(2).max(80),description:z.string().max(500).optional(),skills:z.array(z.string()).max(12).default([]),capacity:z.number().int().min(1).max(20).default(1)});
export async function POST(request:Request,{params}:{params:Promise<{projectId:string}>}){try{const member=await requireMember(),{projectId}=await params;const [project]=await getDb().select({id:projects.id}).from(projects).where(and(eq(projects.id,projectId),eq(projects.ownerId,member.id))).limit(1);if(!project)throw new ApiError(403,"Only a project owner can add roles");const [role]=await getDb().insert(projectRoles).values({...schema.parse(await request.json()),projectId}).returning();await audit(member.id,"project.role_created","project",projectId,{roleId:role.id});return NextResponse.json(role,{status:201})}catch(error){return apiError(error)}}
